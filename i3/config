exec --no-startup-id feh --bg-fill ~/.wallpapers/geo_pattern_even_darker.png

set $mod Mod4

font pango:monospace 10

exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id dunst

exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock -c 000000 --nofork

exec --no-startup-id nm-applet

exec --no-startup-id unclutter -idle 3

exec xinput set-prop "DELL0A20:00 06CB:CE65 Touchpad" "libinput Tapping Enabled" 1
exec xinput set-prop "DELL0A20:00 06CB:CE65 Touchpad" "libinput Natural Scrolling Enabled" 1

bindsym $mod+t exec --no-startup-id toggle_touchpad_i3; exec pkill -SIGRTMIN+11 i3blocks

bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5%; exec pkill -SIGRTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5%; exec pkill -SIGRTMIN+10 i3blocks
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle; exec pkill -SIGRTMIN+10 i3blocks
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle; exec pkill -SIGRTMIN+11 i3blocks

bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +10%; exec pkill -SIGRTMIN+12 i3blocks

bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 10%-; exec pkill -SIGRTMIN+12 i3blocks

focus_follows_mouse no

floating_modifier $mod

tiling_drag modifier titlebar

bindsym $mod+Return exec alacritty

bindsym $mod+Shift+q kill

bindsym $mod+d exec --no-startup-id rofi -show drun -config ~/.config/rofi/config.rasi
bindsym $mod+n exec attempt_switch_to_empty_workspace_i3
bindsym $mod+w exec focus_or_open_firefox_i3

bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

bindsym $mod+b split h
bindsym $mod+v split v

bindsym $mod+f fullscreen toggle

bindsym $mod+e layout toggle split

bindsym $mod+Shift+space floating toggle

bindsym $mod+space focus mode_toggle

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

include $HOME/.machine_config/i3_workspace_to_monitor

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+mod2+KP_1 workspace number $ws1
bindsym $mod+mod2+KP_2 workspace number $ws2
bindsym $mod+mod2+KP_3 workspace number $ws3
bindsym $mod+mod2+KP_4 workspace number $ws4
bindsym $mod+mod2+KP_5 workspace number $ws5
bindsym $mod+mod2+KP_6 workspace number $ws6
bindsym $mod+mod2+KP_7 workspace number $ws7
bindsym $mod+mod2+KP_8 workspace number $ws8

bindsym $mod+comma workspace prev
bindsym $mod+period workspace next

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

bindsym $mod+Shift+c reload

bindsym $mod+Shift+r restart

bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+p exec NOW=$(date +%d-%b-%Y_%H-%M-%S) && maim --format png --select > ~/.screenshots/screenshot_$NOW.png && xclip -selection clip -t image/png ~/screenshots/screenshot_$NOW.png


mode "resize" {
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym j resize grow height 5 px or 5 ppt
        bindsym k resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

set $accent "#bfbdb6"

bar {
    position top

    font pango:JetBrainsMonoNL Nerd Font Mono Bold 10

    status_command i3blocks

    colors {
        background #141c21d8
        statusline #93a1a1
        separator #141c21

        #                 border, bg,     text
        focused_workspace #141c21 $accent #141c21
        active_workspace #141c21 #232d38 #bfbaac
        inactive_workspace #141c21 #141c21 #bfbaac
        urgent_workspace #141c21 #b08500 #bfbaac
    }
}

#              border, fg,     text,   indic,  child_border
client.focused $accent #263640 #93a1a1 $accent $accent
client.focused_inactive #263640 #b08500 #93a1a1 #263640 #263640
client.unfocused #263640 #b08500 #93a1a1 #263640 #263640
client.urgent #263640 #b08500 #93a1a1 #263640 #263640
client.placeholder #263640 #b08500 #93a1a1 #263640 #263640
client.background #263640


set $border_width 2

for_window [class=gwenview|Eog|Sxiv|feh|spectacle|mpv|vlc|ark|File-roller|Xarchiver] floating enable
for_window [class=gwenview|Eog|Sxiv|feh|spectacle|mpv|vlc|ark|File-roller|Xarchiver] focus
for_window [class=".*"] border pixel $border_width
hide_edge_borders smart
gaps inner 5
smart_gaps on

default_border pixel $border_width

bindsym $mod+Shift+Right move workspace to output right
bindsym $mod+Shift+Left move workspace to output left
bindsym $mod+Shift+Down move workspace to output down
bindsym $mod+Shift+Up move workspace to output up
